<li>
  <%= gravatar_for user, size: Settings.validates_name %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t(".delete"), user, method: :delete,
                                  data: { confirm: "You sure?" } %>
  <% end %>
</li>
